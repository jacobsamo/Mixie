import { Info } from '@/src/common/types/recipe';
import React from 'react';

interface InfoProps {
  info: Info;
}

const Info = ({ info }: InfoProps) => {
  return <div>Info</div>;
};

export default Info;
